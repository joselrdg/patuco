const cmpButtons = require("./cmpButtons.js");
const cmpCards = require("./cmpCards.js");
const cmpInput = require("./cmpInput.js");


const baseStyleComponents = {
  cmpButtons,
  cmpInput,
  cmpCards,
};

module.exports = baseStyleComponents;
